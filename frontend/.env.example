# =============================================================================
# P2P Procurement — Frontend environment templates
#
# Setup once:
#   npm run env:init
#
# Run:
#   npm run dev               # uses .env.dev
#   npm run start:prod        # uses .env.prod (after npm run build:prod)
# =============================================================================

See `.env.dev.example` and `.env.prod.example`.
